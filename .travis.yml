language: python

sudo: required

python: 
  - "3.6"

dist: trusty

git:
  depth: 1

install:
  - pip3 install -r requirements.txt
  - yarn add --dev jest

env: 
  - TRAVIS_SECURE_ENV_VARS=true

script:
  - coverage run manage.py test 
  - yarn run test 

after_success:
  - codecov